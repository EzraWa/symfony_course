{% extends 'base.html.twig' %}

{% block title %}Blog Post!{% endblock %}
{% block body %}
    <p>ID {{ id }}</p>
    <h3>{{ post.title | upper }}</h3>
    <p>{{ post.text }}</p>
    <p>{{ post.date | date }}</p>
    <p> {{  560 | price }}</p>
{% endblock %}